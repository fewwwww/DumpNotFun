{
  "name": "dumpnotfun",
  "version": "0.0.1",
  "description": "A memecoin shorting platform deployed on Mode Network",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network mode"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.1.1",
    "@openzeppelin/contracts": "^4.8.0",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.0"
  },
  "devDependencies": {
    "hardhat": "^2.12.0"
  }
}
